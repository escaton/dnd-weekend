## 1. Restructure Monorepo

- [x] 1.1 Create `apps/web/worker/` directory
- [x] 1.2 Move `apps/server/src/auth.ts` to `apps/web/worker/auth.ts` (no changes needed — jose works on Workers)
- [x] 1.3 Move `apps/server/src/context.ts` to `apps/web/worker/context.ts` (update to receive `env` binding parameter)
- [x] 1.4 Move `apps/server/src/db.ts` to `apps/web/worker/db.ts` (rewrite to use `env.HYPERDRIVE.connectionString` instead of `DATABASE_URL`)
- [x] 1.5 Move `apps/server/src/index.ts` to `apps/web/worker/index.ts` (rewrite as Worker fetch handler — remove Hono, `serve()`, CORS, static serving, `/healthz`)
- [x] 1.6 Move `apps/server/migrations/` to `packages/api/migrations/`
- [x] 1.7 Move `apps/server/drizzle.config.ts` to `packages/api/drizzle.config.ts` (update `schema` path to `./src/db/schema.ts`, update `out` to `./migrations`)
- [x] 1.8 Update `packages/api/package.json` — add `drizzle-kit` and `tsx` to devDependencies, add `db:migrate` and `db:generate` scripts
- [x] 1.9 Update `apps/web/package.json` — add `drizzle-orm`, `postgres`, `jose`, `@trpc/server` to dependencies; add `@cloudflare/vite-plugin`, `wrangler` to devDependencies; remove `@hono/node-server` if present
- [x] 1.10 Delete `apps/server/` directory entirely
- [x] 1.11 Delete `Dockerfile`, `fly.prod.toml`, `fly.test.toml`
- [x] 1.12 Remove `hono` and `@hono/node-server` from any remaining package.json (if referenced)

## 2. Configure Vite + Wrangler

- [x] 2.1 Add `@cloudflare/vite-plugin` to `apps/web/vite.config.ts` (import `cloudflare` from `@cloudflare/vite-plugin`, add to plugins array)
- [x] 2.2 Create `apps/web/wrangler.jsonc` with: `name`, `main` pointing to `./worker/index.ts`, `assets` config (`not_found_handling: "single-page-application"`), `hyperdrive` binding (`binding: "HYPERDRIVE"`, `id` test config, `localConnectionString` for local dev), `vars` for non-secret env (`SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`)
- [x] 2.3 Create `.dev.vars` file (gitignored) with local dev secrets (empty — no runtime secrets needed, Worker uses Hyperdrive binding + JWKS only)
- [x] 2.4 Update `.env.example` to document new local dev variables (no `DATABASE_URL` at runtime, no `PORT`, no `SUPABASE_SECRET_KEY` — not used by Worker or client)
- [x] 2.5 Add `.dev.vars` and `.wrangler/` to `.gitignore`
- [x] 2.6 Verify `apps/web/tsconfig.json` includes the `worker/` directory in compilation

## 3. Rewrite Worker Entry Point

- [x] 3.1 Write `apps/web/worker/index.ts` — export default object with `fetch(request, env)` handler calling `fetchRequestHandler({ endpoint: "/api", router: appRouter, req: request, createContext: () => createContext(request, env) })`
- [x] 3.2 Write `apps/web/worker/db.ts` — use `postgres(env.HYPERDRIVE.connectionString)` + `drizzle(client, { schema })` instead of `postgres(DATABASE_URL)`; remove `DATABASE_URL` env var check
- [x] 3.3 Update `apps/web/worker/context.ts` — `createContext(req: Request, env: Env)` creates `db` from `env.HYPERDRIVE`, passes to context; `Env` type generated by `wrangler types`
- [x] 3.4 Verify `apps/web/worker/auth.ts` works as-is on Workers runtime (jose `createRemoteJWKSet` and `jwtVerify` are Worker-compatible — updated to receive `supabaseUrl` param instead of reading `process.env`)

## 4. Verify Local Development

- [x] 4.1 Run `pnpm --filter @dnd-weekend/web dev` — verify Vite dev server starts with workerd runtime (started successfully on localhost:5173)
- [x] 4.2 Spike: verify `@cloudflare/vite-plugin` correctly bundles `@dnd-weekend/api` workspace dependency into the Worker (build succeeded, no module errors)
- [x] 4.3 Spike: verify Hyperdrive local mode connects to test Supabase Postgres via `localConnectionString` in `wrangler.jsonc` (added `localConnectionString` pointing at test Supabase, dev server starts)
- [x] 4.4 Spike: verify `drizzle-orm/postgres-js` accepts `env.HYPERDRIVE.connectionString` value without changes (build and typecheck pass, `postgres(connectionStr)` pattern confirmed by Hyperdrive docs)
- [x] 4.5 Verify tRPC endpoints respond (`/api/character.list`, `/api/auth.me`) — use browser or curl with debug user JWT
- [x] 4.6 Verify no CORS errors in browser dev tools (same-origin, single port)

## 5. Update Migration Runner

- [x] 5.1 Verify `drizzle-kit generate` works from `packages/api/` with new `drizzle.config.ts` paths (runs successfully, detects 1 table, 0 schema changes)
- [x] 5.2 Verify `drizzle-kit migrate` works from `packages/api/` against test Supabase using `DATABASE_URL` from `.env` (migrations applied successfully)
- [x] 5.3 Add `db:migrate` script to `packages/api/package.json` — `tsx --env-file=../../.env src/migrate.ts`
- [x] 5.4 Create `packages/api/src/migrate.ts` — standalone migration runner using `postgres-js` + `drizzle-orm/postgres-js/migrator`, reads `DATABASE_URL` from env

## 6. Update GitHub Workflows

- [x] 6.1 Rewrite `.github/workflows/deploy-test.yml` — checkout, install, run `db:migrate` against test DB (using `DATABASE_URL_TEST` secret), build, then `wrangler deploy`; fail workflow if migrations fail
- [x] 6.2 Rewrite `.github/workflows/deploy-prod.yml` — checkout (optional commit SHA input), install, run `db:migrate` against prod DB (using `DATABASE_URL_PROD` secret), build, configure prod config, then `wrangler deploy`; fail workflow if migrations fail
- [x] 6.3 GitHub secrets created by user: `DATABASE_URL_TEST`, `DATABASE_URL_PROD`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`
- [x] 6.4 Verify `ci.yml` still passes (lint, typecheck, test) — removed `esbuild` rebuild step, all checks pass

## 7. Create Cloudflare Resources

- [x] 7.1 Create Cloudflare project `dnd-weekend-test` (via `wrangler pages project create`)
- [x] 7.2 Create Cloudflare project `dnd-weekend` (via `wrangler pages project create`)
- [x] 7.3 Create Hyperdrive config for test (pointing at test Supabase Postgres connection string)
- [x] 7.4 Create Hyperdrive config for prod (pointing at prod Supabase Postgres connection string)
- [x] 7.5 Update `apps/web/wrangler.jsonc` with real Hyperdrive config IDs (test ID in config, prod ID in deploy workflow)
- [x] 7.6 Set non-secret vars for test (`SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY` in `wrangler.jsonc` vars)
- [x] 7.7 Set non-secret vars for prod (in deploy-prod workflow inline config)
- [x] 7.8 Set secrets for test (`SUPABASE_SECRET_KEY` set via `wrangler pages secret put` — but not used by Worker, orphaned)
- [x] 7.9 Set secrets for prod (same — orphaned, `SUPABASE_SECRET_KEY` not used by Worker or client)

## 8. Verify Build and Deploy

- [x] 8.1 Run `pnpm --filter @dnd-weekend/web build` — verify static assets and Worker bundle are produced
- [ ] 8.2 Trigger deploy-test workflow — verify migrations run, then `wrangler deploy` succeeds, site is live
- [ ] 8.3 Verify test deployment: sign in with debug user, create/list/soft-delete a character
- [ ] 8.4 Trigger deploy-prod workflow — verify migrations run, then deploy succeeds, site is live
- [ ] 8.5 Verify prod deployment: sign in with debug user, create/list/soft-delete a character
- [ ] 8.6 Verify rollback: run `wrangler rollback` and confirm previous deployment is served

## 9. Decommission Old Infrastructure

- [ ] 9.1 Verify Cloudflare deployments are stable and serving traffic correctly
- [ ] 9.2 Delete Fly.io app `dnd-weekend-test`
- [ ] 9.3 Delete Fly.io app `dnd-weekend-prod`
- [ ] 9.4 Remove `FLY_API_TOKEN` from GitHub secrets (no longer needed)
- [x] 9.5 Update root `package.json` scripts if `dev` script references `apps/server` (should now be `pnpm --filter @dnd-weekend/web dev`)
